---
description:
globs:
alwaysApply: true
---

When using React hooks etc like useState, useEffect etc. never use the React.useState<SortingState>([]); syntax, instead import useState and use it like this: useState<SortingState>([]);

Never create components in the app directory, always in the src directory!
